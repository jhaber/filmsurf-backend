var RottenTomatoes = require('./rottentomatoes');
var Persistence = require('./persistence');

exports.retrieve = function(params, callback) {
    RottenTomatoes.retrieve(params.id, function (data) {
        callback(data);
        Persistence.persistMovie(data);
    });
}