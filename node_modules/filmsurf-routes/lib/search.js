var RottenTomatoes = require('./rottentomatoes');

exports.search = function(query, page, items, params, callback) {
    if (query) {
        RottenTomatoes.search(query, page, items, callback);
    } else {
        callback({ search: [] });
    }
}